# Patch 2.74.35

Test-only compatibility fix.

- Updated two Auto Style test fixtures in `src/editableDocument.test.ts` to the current heading node shape (`headingKind`) instead of removed legacy fields (`level`, `section`).
- No production parser, compiler, renderer, or Auto Style behavior changed.
- Version bumped to 2.74.35.
